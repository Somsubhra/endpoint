#ifndef ENDPOINTMAINWINDOW_H
#define ENDPOINTMAINWINDOW_H

#include <QMainWindow>

class EndpointMainWindow : public QMainWindow
{
    Q_OBJECT

public:
    EndpointMainWindow(QWidget *parent = nullptr);
    ~EndpointMainWindow();
};
#endif // ENDPOINTMAINWINDOW_H
